#include<stdio.h>
int main()
{
    int a[60],i,size,k;
    printf("enter the size of the array");
    scanf("%d",&size);
    printf("enter the elements of the array");
    scanf("%d",&a[i]);
    k=a[0];
    for(i=1;i<Lsize;i++)
    {
        if(k<=a[i])
        {
            printf("%d",k);
        }
    }
}
